subprojects {
    group = 'xyz.algogo'

    repositories {
        google()
        mavenCentral()
        jcenter()
        maven {
            url = "https://jitpack.io"
        }
    }

    configurations {
        compile {
            exclude module: 'antlr4'
        }
    }
}